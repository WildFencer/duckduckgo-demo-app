import { HighlightHitPipe } from './highlight-hit.pipe';

describe('HighlightHitPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightHitPipe();
    expect(pipe).toBeTruthy();
  });
});
